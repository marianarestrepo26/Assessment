using CoursePlatform.API.Data;

namespace CoursePlatform.API.Services
{
    // Keeping it simple, standard CRUD usually goes here or directly in controllers for very small apps.
    // The user asked for "Hexagonal" so I will keep logic in Services.
}
